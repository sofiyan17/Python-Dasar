# import os
# os.system("cls")


'''
            KODE TERMINAL
1. py -m venv poject1 = untuk membuat folder virtual invironment dengan nama projcet1
2. ls = untuk melihat file atau folder apa saja yang ada di dalam folder yang kita tempati
3. where py = untuk mengetahui file python kita ada di mana
4. dir = untuk melihat file atau folder apa saja yang ada di dalam folder yang kita tempati
5. activate = untuk mengaktifkan invironment
6. deactive = untuk menonaktifkan invironment
7. cd nama_folder= untuk masuk ke dalam folder
8. cd .. = untuk keluar dari folder
9. pwd = untuk mengetahui kita sedang berada di folder mana
10. nama dan tempat folder/activate = untuk memindahkan python ke folder yang kita mau
11. pip freeze --local > virtualenv.txt = untuk membuat rekaman pip yang sudah kita install bernama virtualenv(bisa apa saja)
12. pip install -r virtualenv.txt = untuk menginstall pip yang sudah kita rekam di virtualenv.txt

'''

import numpy as np

a = np.array([1, 2, 3, 4, 5,])
b = np.array([6, 7, 8, 9, 10,])

print(a + b)

